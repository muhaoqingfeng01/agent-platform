# 前端代码生成 — 完整执行话术

> 版本: 1.0.0 | 日期: 2026-06-22
> 用途: 逐批复制粘贴到对话中，驱动 Claude 按三份文档生成前端代码
> 覆盖: agent-platform-API.md + 前后端权限对应技术方案.md + 前端架构设计方案.md

---

## 使用方式

每批次一个代码块，**复制 → 粘贴到对话 → 等待 Claude 生成 → 验证 → 下一批**。
每批 ≤20 文件，26 批跑完全量。

---

## 第零步：确认环境（仅首次对话）

```
我准备开始 Agent Platform 前端项目开发。请先确认以下文档在你上下文中：

1. docs/api-documentation/agent-platform-API.md（API 数据契约）
2. docs/api-documentation/前后端权限对应技术方案.md（权限模型）
3. docs/api-documentation/前端架构设计方案.md（架构规范）

请回复你已读取这三份文档，并列出你看到的主要章节标题。
```

**验证**：Claude 回复了文档章节 → 确认上下文就绪 → 进入 Batch 1-1

---

---

# Phase 1：基础底座

---

## Batch 1-1 — 项目初始化 + 配置体系

```
【Batch 1-1】项目脚手架 + 配置体系 + HTTP 客户端，预计 18 个文件。

【文档锚点】
- 前端架构设计方案.md §二（技术选型表）
- 前端架构设计方案.md §三（目录结构）
- 前端架构设计方案.md §四（配置化设计 + Axios 实例）
- 前端架构设计方案.md §5.1.1（通用 API 响应类型 Result<T> + PageResponse<T>）
- 前端架构设计方案.md §十一（开发规范）

【生成清单】
1. package.json（锁定 §2.1 的 17 项依赖版本：React 18/TS 5/Vite 5/Ant Design 5.x/
   Zustand 4/React Router 6/Axios 1/TanStack Query 5/react-window 3/
   react-markdown 9/react-syntax-highlighter 15/@ant-design/charts 2/
   @xyflow/react 12/dayjs 1/React Hook Form 7/@ant-design/icons 5/
   ESLint+Prettier，禁止引入其他）
2. tsconfig.json（strict: true + path alias @/*）
3. vite.config.ts（代理 /api → localhost:8080 + /ws → localhost:8080 + 构建配置）
4. .env.development（VITE_API_BASE_URL=http://localhost:8080 + VITE_WS_URL=ws://localhost:8080/ws）
5. .env.production（VITE_API_BASE_URL=https://api.example.com + VITE_WS_URL=wss://api.example.com/ws）
6. .eslintrc.js + .prettierrc
7. index.html
8. public/config.json（完整 schema：api/baseURL/timeout/retryCount/retryDelay +
   websocket/url/heartbeatInterval/reconnectAttempts/reconnectDelay +
   features/enableKnowledgeBase/enableTaskPlanning/enableOptimization/enableObservability +
   chat/maxMessageLength/pageSize/streamTimeout +
   upload/maxFileSize/allowedTypes + im/enabledChannels/wecom/dingtalk）
9. src/vite-env.d.ts
10. src/main.tsx（先用 placeholder，后续注入 App）
11. src/App.tsx（空壳）
12. src/config/app.config.ts（loadAppConfig + getAppConfig，AppConfig 接口类型完整）
13. src/config/constants.ts（API_PREFIX='/api/v1' + CHAT_CONFIG + UPLOAD_CONFIG）
14. src/services/apiClient.ts（Axios 实例 + 请求拦截器 Bearer Token + X-Tenant-Id +
    响应拦截器 401→跳登录/403→提示权限不足/5xx→提示服务器错误）
15. src/types/api.d.ts（Result<T>: code/message/data/timestamp/success +
    PageResponse<T>: records[]/total/page/size/pages）
16. src/assets/styles/global.less
17. src/assets/styles/variables.less

【输出后必须执行】
1. 列出本次 17 个生成文件的路径 + 用途一行说明
2. 自检（逐项打勾）：
   □ 依赖版本是否与 §2.1 表一致（React 18/TS 5/Vite 5/Ant Design 5/Zustand 4）
   □ 目录结构是否与 §三 一致
   □ config.json 是否包含 api/websocket/features/chat/upload/im 所有段
   □ apiClient 拦截器是否处理 401/403/5xx
   □ Result<T>.data 字段 / PageResponse<T>.records 字段是否与后端一致
3. 初始化开发进度：
   在 docs/api-documentation/ 下创建「前端开发进度.md」，写入初始骨架：

   | # | 批次 | 模块 | 文件数 | 状态 | 完成日期 |
   |:--:|------|------|:--:|:--:|------|
   | 1 | 1-1 | 项目脚手架+配置 | 17 | ✅ | YYYY-MM-DD |
   | 2 | 1-2 | 工具函数+路由壳 | — | ⬜ | — |
   | ... | ... | ... | ... | ⬜ | ... |

   汇总行：总批次 26 | 已完成 1 | API 端点 0/XXX | 总文件 17
```

---

## Batch 1-2 — 工具函数 + 错误处理 + 路由壳子

```
【Batch 1-2】工具函数 + ErrorBoundary + 空路由框架，预计 12 个文件。

【文档锚点】
- 前端架构设计方案.md §9.1（ErrorBoundary 类组件）
- 前端架构设计方案.md §9.3（Logger 工具类）
- 前端架构设计方案.md §8（存储/缓存占位）

【生成清单】
1. src/utils/logger.ts（Logger 类：DEBUG/INFO/WARN/ERROR 四级 + 生产环境 sendBeacon +
   timestamp/url/userAgent 上报）
2. src/utils/format.ts（formatDate/formatNumber/formatFileSize/formatRelativeTime）
3. src/utils/validate.ts（isEmail/isPhone/isUrl/isNotEmpty/hasMinLength）
4. src/utils/storage.ts（get/set/remove/clear，localStorage + sessionStorage 封装）
5. src/utils/errorHandler.ts（handleApiError/handleComponentError/captureException）
6. src/utils/index.ts（统一导出）
7. src/components/ErrorBoundary/ErrorBoundary.tsx（React Class 组件 + getDerivedStateFromError +
   Result 500 + 刷新按钮）
8. src/components/ErrorBoundary/index.ts
9. src/config/routes.tsx（空路由壳子：{path:'/login', element: null}、
   {path:'/403', element: null}、{path:'/*', element: null}，后续批次填充）
10. src/hooks/useDebounce.ts
11. src/hooks/useLocalStorage.ts
12. src/hooks/index.ts

【输出后必须执行】
1. 列出本次 12 个生成文件的路径 + 用途一行说明
2. 自检：
   □ Logger 四级别是否齐全
   □ ErrorBoundary 是否实现 getDerivedStateFromError
   □ routes.tsx 是否预留 login/403/404 占位
3. 更新 docs/api-documentation/前端开发进度.md（Batch 1-2 标记 ✅，汇总更新）
```

---

## Batch 1-3 — 主布局框架

```
【Batch 1-3】主布局 + 私有路由守卫，预计 10 个文件。

【文档锚点】
- 前端架构设计方案.md §7.1（聊天页面布局 ASCII 图：Header + Sidebar + Content + InputArea）
- 前后端权限对应技术方案.md §4.2（X-Tenant-Id Header）

【生成清单】
1. src/components/Layout/MainLayout.tsx（Ant Design Layout：Sider+collapsible +
   Header+用户下拉 + Content+ErrorBoundary 包裹 + Outlet）
2. src/components/Layout/Header.tsx（Logo + 右侧：租户名称 + 用户头像Dropdown + 登出）
3. src/components/Layout/Sidebar.tsx（空菜单壳子，Ant Design Menu inline 模式，
   onClick navigate，selectedKeys 联动 location.pathname，无权限过滤，后续 2-2 增强）
4. src/components/Layout/index.ts
5. src/router/PrivateRoute.tsx（读取 useAuthStore.isAuthenticated →
   未登录跳 /login？redirect=当前路径 → 已登录渲染 Outlet）
6. src/router/index.ts
7. src/pages/NotFound/NotFoundPage.tsx（Result 404 + 返回首页按钮）
8. src/pages/NotFound/index.ts
9. 更新 src/App.tsx（BrowserRouter + Routes + PrivateRoute 包裹 Layout + NotFound）
10. 更新 src/config/routes.tsx（实际路由：/ → PrivateRoute → Layout → children 占位；
    /login 公开；/403 公开；/* → NotFound；所有页面懒加载 Suspense）

【输出后必须执行】
1. 列出本次 10 个文件的路径 + 用途
2. 自检：
   □ MainLayout 是否包含 Header/Sidebar/Content/ErrorBoundary 四层
   □ PrivateRoute 是否检查 token + 记录 redirect 路径
   □ NotFound 是否有返回首页按钮
3. 更新 docs/api-documentation/前端开发进度.md（Batch 1-3 ✅，Phase 1 汇总：
   总文件 39，基础底座 100%）
```

---

---

# Phase 2：认证与权限

---

## Batch 2-1 — 认证流程

```
【Batch 2-1】登录/登出/Token刷新 + 登录页面，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §认证管理（精确 4 端点）：
  POST /api/v1/auth/login — LoginRequest{username,password,tenantId,provider?}
  GET /api/v1/auth/me — 返回 UserInfo{userId,username,tenantId,roles[],permissions[]}
  POST /api/v1/auth/refresh — {userId,refreshToken} → {token,refreshToken}
  POST /api/v1/auth/logout — 无请求体
- 前后端权限对应技术方案.md §2.1（auth.ts 完整代码 + LoginRequest/LoginResponse/UserInfo 类型）
- 前后端权限对应技术方案.md §3.1（登录时序图：login→存token→loadUserInfo→存permissions）
- 前后端权限对应技术方案.md §4.2（useAuthStore：token/refreshToken/userId/tenantId/isAuthenticated）

【生成清单】
1. src/types/auth.d.ts（LoginRequest: username+password+tenantId+provider? /
   LoginResponse: token+refreshToken+user? / UserInfo: userId+username+tenantId+roles[]+permissions[]）
2. src/services/auth.ts（authService.login/logout/refresh/loadUserInfo，
   路径全部用 `${API_PREFIX}/auth/...`，
   login 成功后自动调用 loadUserInfo 存权限，
   refresh 成功后重新 loadUserInfo）
3. src/stores/useAuthStore.ts（token/refreshToken/userId/tenantId +
   setToken/setRefreshToken/setUserId/setTenantId/logout +
   get isAuthenticated(): boolean）
4. src/pages/Login/LoginPage.tsx（Ant Design Form：
   租户 ID InputNumber + 用户名 Input + 密码 Password +
   登录方式 Radio: LOCAL/LDAP/SSO + 登录按钮 loading 态 +
   错误 Alert + 成功后 navigate(redirect || '/chat')）
5. src/pages/Login/index.ts
6. 更新 src/services/apiClient.ts（请求拦截器从 useAuthStore.getState() 读取
   token→Authorization Bearer + tenantId→X-Tenant-Id；响应拦截器 401→
   useAuthStore.logout()+跳/login）
7. 更新 src/router/PrivateRoute.tsx（接入 useAuthStore.isAuthenticated，
   未登录 Navigate to="/login" state={{from:location}}）
8. 更新 src/config/routes.tsx（/login 路由指向 LoginPage）
9. 更新 src/main.tsx（loadAppConfig() → createRoot → render App）

【输出后必须执行】
1. 列出本次生成/更新的文件路径 + 用途
2. 自检：
   □ POST /api/v1/auth/login 参数名是否完全匹配 agent-platform-API.md
   □ GET /api/v1/auth/me 返回字段是否包含 roles[]+permissions[]
   □ POST /api/v1/auth/refresh 参数是否 {userId, refreshToken}
   □ login 成功后是否自动调用 setPermissions+setRoles
   □ 401 拦截是否 logout + 跳 /login
3. 更新 docs/api-documentation/前端开发进度.md（Batch 2-1 ✅）
```

---

## Batch 2-2 — RBAC 权限基础设施

```
【Batch 2-2】权限 Store + 三层权限控制 + 动态菜单，预计 14 个文件。

【文档锚点】
- 前后端权限对应技术方案.md §1.3（权限编码规范：24 种 {resource}:{action} + 4 角色）
- 前后端权限对应技术方案.md §2.2（usePermissionStore 完整 Zustand 代码）
- 前后端权限对应技术方案.md §2.3（PermissionRoute 路由守卫组件）
- 前后端权限对应技术方案.md §2.4（Sidebar 动态菜单 + menuConfig 11 项 + filterMenus）
- 前后端权限对应技术方案.md §2.5（Authorized 组件 + AuthorizedButton）
- 前后端权限对应技术方案.md §4.1（SUPER_ADMIN 全通逻辑）
- 前后端权限对应技术方案.md §4.3（localStorage 持久化 + 权限刷新策略）

【生成清单】
1. src/stores/usePermissionStore.ts（Zustand + persist：
   permissions[]+roles[]+setPermissions+setRoles+
   hasPermission→SUPER_ADMIN 全通 + hasRole+hasAnyPermission+
   hasAllPermissions+clearPermissions，localStorage key='agent-platform-permission'）
2. src/hooks/usePermission.ts（usePermission/useRole/useAnyPermission/useAllPermissions）
3. src/router/PermissionRoute.tsx（Props: requiredPermission?/requiredPermissions[]?/
   requireAll?/requiredRole? → 未登录跳/login → 无权限跳/403 → 有权限渲染children）
4. src/components/Authorized/Authorized.tsx（Props: permission?/permissions[]?/
   requireAll?/role?/fallback? → hasAccess？children:fallback）
5. src/components/Authorized/AuthorizedButton.tsx（extends ButtonProps +
   permission/permissions/role → 无权限 return null）
6. src/components/Authorized/index.ts
7. 更新 src/components/Layout/Sidebar.tsx（menuConfig 11 项：每项 key/label/icon/
   permission?/role? → filterMenus 函数权限过滤 → Ant Design Menu items）
8. 更新 src/services/auth.ts（login 成功后调用 usePermissionStore.setPermissions+
   setRoles；refresh 后重新 loadUserInfo 刷新权限）
9. 更新 src/config/routes.tsx（所有路由占位包裹 PermissionRoute + 权限标识：
   /chat→conversation:read, /knowledge→knowledge:read, /tools→tool:read,
   /tasks→task:read, /prompts→prompt:read, /approvals→approval:read,
   /users→user:read, /security→security:read, /optimization→optimization:read,
   /evaluation→evaluation:read, /tenants→role:TENANT_ADMIN）
10. 更新 src/hooks/index.ts

【输出后必须执行】
1. 列出本次 10 个生成/更新文件的路径 + 用途
2. 自检：
   □ 权限标识是否全部使用 {resource}:{action} 格式
   □ SUPER_ADMIN 是否全通
   □ Sidebar 11 菜单项 × 权限标识是否与 §2.4 menuConfig 一致
   □ PermissionRoute 未登录→/login / 无权限→/403
   □ AuthorizedButton 无权限是否 return null
3. 输出"权限覆盖矩阵"表：| 菜单项 | 路由 | 权限标识 | 角色 |
4. 更新 docs/api-documentation/前端开发进度.md（Batch 2-2 ✅）
```

---

## Batch 2-3 — 用户 + 角色 + 权限 + 租户页面

```
【Batch 2-3】四个管理页面 + Service 层，预计 18 个文件。

【文档锚点】
- agent-platform-API.md §用户管理（7 端点）：POST /api/v1/users(注册)、
  GET /api/v1/users(列表)、GET /api/v1/users/{id}(详情)、
  PUT /api/v1/users/{id}(更新)、PATCH /api/v1/users/{id}/status(启停)、
  PUT /api/v1/users/{id}/password(改密码)
- agent-platform-API.md §角色管理（6 端点）：GET/POST /api/v1/roles、
  GET /api/v1/roles/{id}、PUT /api/v1/roles/{id}、DELETE /api/v1/roles/{id}、
  GET /api/v1/roles/{id}/users、POST /api/v1/roles/{id}/users、
  POST /api/v1/roles/{id}/permissions
- agent-platform-API.md §权限管理（2 端点）：GET/POST /api/v1/permissions
- agent-platform-API.md §租户管理（端点去重后 5 个）：GET/POST /api/v1/tenants、
  GET/PUT/DELETE /api/v1/tenants/{id}、PATCH /api/v1/tenants/{id}/status
- 前后端权限对应技术方案.md §4.2（租户隔离：X-Tenant-Id）

【生成清单】
1. src/types/user.d.ts（UserResponse/CreateUserRequest/UpdateUserRequest/
   ChangePasswordRequest）
2. src/types/role.d.ts（RoleResponse/CreateRoleRequest/UpdateRoleRequest/
   AssignRoleRequest/AssignPermissionRequest）
3. src/types/permission.d.ts（PermissionResponse）
4. src/types/tenant.d.ts（TenantResponse/CreateTenantRequest/UpdateTenantRequest）
5. src/services/users.ts（7 端点）
6. src/services/roles.ts（6 端点）
7. src/services/permissions.ts（2 端点）
8. src/services/tenants.ts（5 端点）
9. src/pages/Users/UserListPage.tsx + index.ts（Table + 搜索username/email +
   新建/编辑 Modal + 启停 Switch + 改密码 Modal + 所有操作按钮 AuthorizedButton
   permission="user:write"）
10. src/pages/Users/RoleManagement.tsx + index.ts（Table 角色列表 + 创建/编辑 Modal +
    行内"分配用户"按钮 → Drawer 用户列表 + Checkbox 选择 +
    "分配权限"按钮 → Tree 组件 24 种权限编码勾选）
11. src/pages/Users/PermissionConfig.tsx + index.ts（只读 Table 权限列表展示）
12. src/pages/Tenants/TenantListPage.tsx + index.ts（PermissionRoute
    requiredRole="TENANT_ADMIN" + Table + 新建/编辑/启停/删除）

【输出后必须执行】
1. 列出本次 18 个文件的路径 + 用途
2. 输出"API 端点对照表"：| Service 函数 | HTTP 方法 | API 路径 | agent-platform-API.md 章节 |
3. 自检：
   □ 所有 API 路径参数名是否与 agent-platform-API.md 一致
   □ RoleManagement 权限树是否覆盖 24 种权限编码
   □ TenantListPage 是否仅 TENANT_ADMIN/SUPER_ADMIN 可见
4. 更新 docs/api-documentation/前端开发进度.md（Batch 2-3 ✅，Phase 2 汇总：
   总文件 45，认证权限 100%，API 端点对接 4+15=19）
```

---

---

# Phase 3：核心对话

---

## Batch 3-1 — 会话类型 + Service + Store

```
【Batch 3-1】会话管理 + 消息模型 + 服务层，预计 16 个文件。

【文档锚点】
- agent-platform-API.md §对话管理（9 端点）：GET /api/v1/conversations(列表)、
  POST /api/v1/conversations(创建)、GET /api/v1/conversations/{id}(详情)、
  PATCH /api/v1/conversations/{id}/title(标题)、
  PATCH /api/v1/conversations/{id}/status(状态流转)、
  DELETE /api/v1/conversations/{id}(逻辑删除)、
  POST /api/v1/conversations/{id}/messages(非流式)、
  GET /api/v1/conversations/{id}/messages(历史)、
  GET /api/v1/conversations/{id}/messages?before={id}(更早消息)、
  POST /api/v1/conversations/{id}/stream(SSE流式)、
  PATCH /api/v1/conversations/{id}/messages/{msgId}/feedback(反馈)
- 前端架构设计方案.md §5.1.2（统一消息模型 + MessageType 联合 + MessageStatus 状态机）
- 前端架构设计方案.md §5.2（conversation service + message service 示例代码）
- 前端架构设计方案.md §5.3.1（SSEEvent 联合类型 8 种 + TokenUsage）

【生成清单】
1. src/types/conversation.d.ts（Conversation/ConversationStatus 枚举/
   CreateConversationRequest/UpdateTitleRequest/StatusTransitionRequest）
2. src/types/message.d.ts（BaseMessage: id/conversationId/type/timestamp/status/channel? +
   MessageType: 'message'|'approval'|'feedback'|'system' +
   MessageStatus 状态机: pending→sent→delivered→read + failed 重试 +
   ChatMessage: type:'message'+role:'user'|'assistant'+content+metadata? +
   ApprovalMessage: type:'approval'+approvalId+executionId+title+detail+
   riskLevel+requestedBy+options[]+timeout+timeoutAt+createdAt+result?+metadata? +
   FeedbackMessage: type:'feedback'+targetMessageId+rating:1|-1+reason? +
   SystemMessage: type:'system'+level:'info'|'warning'|'error'+content +
   联合类型 Message = ChatMessage|ApprovalMessage|FeedbackMessage|SystemMessage）
3. src/types/sse.d.ts（SSEEvent 联合：{type:'start',messageId} |
   {type:'token',content} | {type:'tool_call',toolName,args} |
   {type:'tool_result',toolName,result} | {type:'thinking',content} |
   {type:'error',code,message} | {type:'done',usage:TokenUsage} |
   {type:'approval_required',approvalId} + TokenUsage: inputTokens/outputTokens/totalTokens）
4. src/services/conversation.ts（9 端点，路径全部用 `${API_PREFIX}/conversations/...`）
5. src/services/message.ts（send/list/loadBefore/feedback）
6. src/stores/useConversationStore.ts（conversations[]/currentConversationId +
   fetchList/createConversation/deleteConversation/updateTitle/transitionStatus）
7. src/stores/useChatStore.ts（messages[]/isStreaming/currentMessage +
   addMessage/updateLastMessage/appendToLastMessage/setStreaming/clearMessages）
8. src/hooks/useStreamMessage.ts（SSE 事件→Store 转换：token→appendToLastMessage、
   tool_call→插入工具摘要消息、tool_result→更新工具结果、
   thinking→折叠、error→错误提示+停止流式、done→标记delivered、
   approval_required→插入ApprovalMessage到消息流）

【输出后必须执行】
1. 列出文件清单 + 用途
2. 输出"消息模型覆盖清单"：4 种 MessageType / 5 种 MessageStatus / 8 种 SSEEvent
3. 自检：
   □ Message 联合类型是否包含 ChatMessage|ApprovalMessage|FeedbackMessage|SystemMessage
   □ SSEEvent 是否 8 种全覆盖
   □ conversation.ts 是否覆盖全部 9 端点
4. 更新 docs/api-documentation/前端开发进度.md（Batch 3-1 ✅）
```

---

## Batch 3-2 — SSE + WebSocket 通信层

```
【Batch 3-2】流式对话 + 实时通信 Hooks，预计 9 个文件。

【文档锚点】
- 前端架构设计方案.md §5.3（useSSE Hook 完整代码：EventSource 连接 + token 传参 +
  onopen/onmessage/onerror + done 自动断开）
- 前端架构设计方案.md §5.3.1（SSE 事件处理流程）
- 前端架构设计方案.md §5.4（useWebSocket Hook 完整代码：心跳 30s +
  指数退避 [1s/2s/4s/8s/16s] 最多 5 次 + sendMessage）
- 前端架构设计方案.md §5.4.1（WS 消息类型对照表 + 断线重连机制 +
  重连后补发消息 + 拉取待审批工单）

【生成清单】
1. src/hooks/useSSE.ts（EventSource 连接 url?token=xxx + onopen 回调 +
   onmessage JSON.parse → 事件分发 → 7 种 SSEEvent 处理 +
   done 自动 close + onerror 自动重连 + disconnect 清理）
2. src/hooks/useWebSocket.ts（new WebSocket(url?token=xxx) + onopen 启动心跳
   setInterval 30s send 'ping' + onmessage 解析 JSON → pong 忽略 / 其他分发 +
   onclose 指数退避重连 scheduleReconnect + onerror 日志 +
   sendMessage JSON.stringify + disconnect 清理 timer+ws +
   重连后 pendingMessages 队列补发 + fetchPendingApprovals）
3. src/hooks/useStreamMessage.ts（已在 3-1 创建，本批完善对接 useSSE）
4. src/hooks/useUpload.ts（XHR upload progress + onprogress 回调 + abort 取消）
5. 更新 src/hooks/index.ts（导出 useSSE/useWebSocket/useStreamMessage/useUpload）

【输出后必须执行】
1. 列出 9 个文件的路径 + 用途
2. 输出"SSE 事件覆盖清单"：| 事件 type | 处理逻辑 | 状态 |
   （start/token/tool_call/tool_result/thinking/error/done/approval_required）
3. 输出"WebSocket 消息覆盖清单"：
   服务端→客户端：approval_card/approval_result/task_progress/system_notification/
   permission_changed/pong
   客户端→服务端：approval_response/chat_message/ping
4. 自检：
   □ 重连延迟数组是否 [1000,2000,4000,8000,16000]
   □ 心跳间隔是否 30000ms
   □ 断线后是否补发队列消息 + 拉取待审批工单
5. 更新 docs/api-documentation/前端开发进度.md（Batch 3-2 ✅）
```

---

## Batch 3-3 — 聊天 UI 组件

```
【Batch 3-3】聊天页面 + 消息组件 + 审批卡片 + 反馈按钮，预计 16 个文件。

【文档锚点】
- 前端架构设计方案.md §7.1（聊天页面完整布局 ASCII 图 + 风险等级定义表 +
   审批卡片交互规则：倒计时/最后60s变红/超时自动拒绝/WS断开禁用按钮）
- 前端架构设计方案.md §8.3（虚拟滚动 VariableSizeList 完整代码 + sizeMap +
   resetAfterIndex + 顶部触发 loadMore）
- 前后端权限对应技术方案.md §1.4（会话/消息权限映射：conversation:read/send/create/update/delete）

【生成清单】
1. src/components/Chat/MessageBubble.tsx（switch(message.type):
   'message'→Markdown react-markdown + react-syntax-highlighter + 用户/AI 左右对齐 +
   'approval'→ApprovalCard / 'feedback'→反馈摘要 / 'system'→Alert 通知样式 +
   每条 AI 消息下方 FeedbackButtons）
2. src/components/Chat/MessageList.tsx（VariableSizeList: height=windowHeight-200 +
   itemCount=messages.length + itemSize=getItemSize(sizeMap缓存) +
   onItemsRendered 顶部触发 loadMore + useEffect 新消息 scrollToBottom +
   onItemRendered 更新 sizeMap + resetAfterIndex）
3. src/components/Chat/InputArea.tsx（TextArea autoSize + 发送按钮 +
   流式时显示 Stop 按钮 + 字符计数 maxMessageLength(4000) +
   上传附件按钮 + Shift+Enter 换行 / Enter 发送）
4. src/components/Chat/TypingIndicator.tsx（三点跳动动画）
5. src/components/Chat/index.ts
6. src/components/CodeBlock/CodeBlock.tsx（react-syntax-highlighter +
   语言标签 + 复制按钮 + Copy 成功提示）
7. src/components/CodeBlock/index.ts
8. src/components/ApprovalCard/ApprovalCard.tsx（RISK_CONFIG 四色：low#52c41a/
   medium#faad14/high#ff4d4f/critical#ff4d4f+animated +
   倒计时进度条 Progress strokeColor 最后60s变红 +
   剩余时间 display + timeout 自动触发拒绝 +
   isWsConnected? 启用按钮:禁用+Tooltip"连接已断开" +
   同意/拒绝 Button + 操作中 loading）
9. src/components/ApprovalCard/index.ts
10. src/components/FeedbackButtons/FeedbackButtons.tsx（点赞👍/点踩👎 IconButton +
    点踩弹出 Reason 下拉：inaccurate/incomplete/unsafe/irrelevant/format_issue/other +
    补充说明 TextArea ≤500字 + 提交/取消 + 已反馈态灰色禁用）
11. src/components/FeedbackButtons/index.ts
12. 更新 src/components/index.ts
13. src/pages/Chat/ChatPage.tsx + index.ts（MainLayout中的主体：
    ConversationSidebar 左 + MessageList 中 + InputArea 下 +
    同时对接 useWebSocket 接收 approval_card→插入消息流 +
    approval_result→更新 ApprovalCard 状态）
14. src/pages/Chat/ConversationSidebar.tsx + index.ts（会话列表 + 新建会话 Button +
    搜索 Input + 删除 Popconfirm + 当前选中高亮 + 标题显示）

【输出后必须执行】
1. 列出 16 个文件的路径 + 用途
2. 输出"消息类型渲染矩阵"：| MessageType | 渲染组件 | 说明 |
3. 自检：
   □ 虚拟滚动是否使用 VariableSizeList（非 FixedSizeList）
   □ RiskLevel 四色是否 low#52c41a/medium#faad14/high#ff4d4f/critical#ff4d4f+animated
   □ 审批倒计时是否最后 60s 变红
   □ WS 断开是否禁用审批按钮
   □ DislikeReason 是否 6 枚举全覆盖
4. 更新 docs/api-documentation/前端开发进度.md（Batch 3-3 ✅，Phase 3 汇总：
   核心对话 100%，总文件 41）
```

---

---

# Phase 4：知识引擎

---

## Batch 4-1 — 知识库类型 + Service + 列表 + 详情

```
【Batch 4-1】知识库 CRUD + 精度配置 + 启用/弃用，预计 12 个文件。

【文档锚点】
- agent-platform-API.md §知识库管理（11 端点）：GET/POST /api/v1/knowledge-bases、
  GET/PUT/DELETE /api/v1/knowledge-bases/{id}、
  PATCH /api/v1/knowledge-bases/{id}/precision（精度参数）、
  PATCH /api/v1/knowledge-bases/{id}/enable、PATCH .../deprecate、
  PATCH /api/v1/knowledge-bases/{id}/chunk-strategy（切片策略）、
  GET /api/v1/knowledge-bases/{id}/stats（文档统计）、
  GET /api/v1/knowledge-bases/{id}/precision-config（精度查询）
- 前端架构设计方案.md §7.2（知识库页面布局 ASCII 图）

【生成清单】
1. src/types/knowledge.d.ts（KnowledgeBaseResponse/CreateKBRequest/UpdateKBRequest/
   PrecisionConfig/ChunkStrategy/KBStats/KBStatus 枚举）
2. src/services/knowledge.ts（11 端点）
3. src/stores/useKnowledgeStore.ts
4. src/pages/KnowledgeBase/KBListPage.tsx + index.ts（Table + 搜索 + 状态筛选 +
   新建 Button AuthorizedButton permission="knowledge:create" +
   每行：文档数/状态Tag/启用-弃用 Switch + 操作：编辑/级联删除）
5. src/pages/KnowledgeBase/KBDetailPage.tsx + index.ts（Descriptions 详情 +
   编辑 Modal + 精度配置 Form：chunkSize/chunkOverlap/topK/threshold +
   切片策略设置 + 启用/弃用/级联删除操作）
6. src/pages/KnowledgeBase/KBStats.tsx + index.ts（文档统计面板：StatCard 总数+
   已解析/解析中/失败 + 切片总数 + 存储大小）

【输出后必须执行】
1. 列出 12 个文件路径 + 用途
2. 输出"KB API 端点对照表"：| 函数 | 方法 | 路径 | agent-platform-API.md 章节 |
3. 自检：
   □ 精度配置字段（chunkSize/chunkOverlap/topK/threshold）是否与 API 一致
   □ 知识库状态枚举（ENABLED/DISABLED/DEPRECATED）是否与 API 一致
4. 更新 docs/api-documentation/前端开发进度.md（Batch 4-1 ✅）
```

---

## Batch 4-2 — 文档管理

```
【Batch 4-2】文档上传 + 解析 + 切片管理 + 文件管理，预计 14 个文件。

【文档锚点】
- agent-platform-API.md §文档管理（10 端点）：POST /api/v1/documents/upload
  （multipart/form-data）、POST /api/v1/documents/batch-parse（批量触发解析）、
  POST /api/v1/documents/{id}/parse（异步解析）、
  PATCH /api/v1/documents/{id}/deprecate、GET /api/v1/documents/list?kbId=、
  GET /api/v1/documents/{id}、DELETE /api/v1/documents/{id}、
  GET /api/v1/documents/{id}/status、GET /api/v1/documents/{id}/download、
  GET /api/v1/documents/{id}/chunks
- agent-platform-API.md §文件管理（2 端点）：GET /api/v1/files/list、
  GET /api/v1/files/status-summary

【生成清单】
1. src/types/document.d.ts（DocumentResponse/UploadRequest/DocumentStatus 枚举/
   ChunkResponse/DocumentStats/FileStatusSummary）
2. src/services/documents.ts（10 端点，upload 用 FormData + multipart +
   onUploadProgress 回调）
3. src/services/files.ts（2 端点）
4. src/pages/KnowledgeBase/DocumentUpload.tsx + index.ts（Ant Design Upload.Dragger +
   进度条 + 批量选择 + 文件类型校验 allowedTypes + 大小校验 maxFileSize）
5. src/pages/KnowledgeBase/DocumentList.tsx + index.ts（Table + 状态标签：PENDING_PARSE/
   PARSING/COMPLETED/FAILED + 下载按钮 + 弃用/删除操作 +
   批量触发解析按钮）
6. src/pages/KnowledgeBase/ChunkPreview.tsx + index.ts（切片列表 + 内容展开 +
   字符数 + 手动删除切片）
7. src/pages/KnowledgeBase/FileStatusSummary.tsx + index.ts（文件状态汇总卡片）

【输出后必须执行】
1. 列出 14 个文件路径 + 用途
2. 自检：
   □ 文件上传是否使用 multipart/form-data
   □ 文档状态枚举是否与 API 一致（PENDING_PARSE/PARSING/COMPLETED/FAILED）
   □ 下载端点是否通过 MinIO 流式代理
3. 更新 docs/api-documentation/前端开发进度.md（Batch 4-2 ✅）
```

---

## Batch 4-3 — 知识检索

```
【Batch 4-3】混合检索 + 命中记录 + 人工标注 + 检索策略，预计 10 个文件。

【文档锚点】
- agent-platform-API.md §知识检索（4 端点）：POST /api/v1/knowledge/search（混合检索）、
  GET /api/v1/knowledge/hit-records（命中记录）、
  PATCH /api/v1/knowledge/hit-records/{id}/annotate（人工标注）、
  GET /api/v1/knowledge/search-strategies（策略预设列表）
- 前端架构设计方案.md §7.2（检索测试功能模块 5）

【生成清单】
1. src/types/search.d.ts（SearchRequest: query+kbId+strategy?+topK?+threshold?+
   vectorWeight?+keywordWeight? / SearchResponse: results[]+totalHits+fusionMethod+
   latency / HitRecordResponse / AnnotationRequest: label 枚举 EXCELLENT/
   NEEDS_FIX/SUPPLEMENT / SearchStrategy）
2. src/services/search.ts（4 端点）
3. src/pages/KnowledgeBase/SearchTest.tsx + index.ts（搜索框 + 知识库 Select +
   策略 Select + topK/threshold/向量权重/关键词权重 Slider +
   结果列表：内容片段+相关度分数+来源文档 + RRF 融合标注 +
   耗时显示 + 高亮匹配关键词）
4. src/pages/KnowledgeBase/HitRecords.tsx + index.ts（命中记录 Table + 时间范围 +
   Label 标签筛选 + 标注下拉：EXCELLENT/NEEDS_FIX/SUPPLEMENT）
5. src/pages/KnowledgeBase/SearchStrategyConfig.tsx + index.ts（策略预设列表只读展示）

【输出后必须执行】
1. 列出 10 个文件路径 + 用途
2. 自检：
   □ 检索参数（vectorWeight/keywordWeight/RRF）与 API 一致
   □ 标注枚举 EXCELLENT/NEEDS_FIX/SUPPLEMENT 与 API 一致
3. 更新 docs/api-documentation/前端开发进度.md（Batch 4-3 ✅，Phase 4 汇总：
   知识引擎 100%，总文件 36）
```

---

---

# Phase 5：Agent 能力

---

## Batch 5-1 — 工具平台（类型 + Service）

```
【Batch 5-1】工具注册 + 版本管理（类型 + Service 层），预计 10 个文件。

【文档锚点】
- agent-platform-API.md §工具平台（10 端点）：GET /api/v1/tools/{id}、
  PUT /api/v1/tools/{id}、GET /api/v1/tools/list?type=、
  POST /api/v1/tools、POST /api/v1/tools/{id}/test、
  POST /api/v1/tools/{id}/rollback?targetVersion=、
  PATCH /api/v1/tools/{id}/status、GET /api/v1/tools/{id}/versions、
  GET /api/v1/tools/{id}/versions/{versionId}、
  GET /api/v1/tools/{id}/invocation-logs

【生成清单】
1. src/types/tool.d.ts（ToolResponse/ToolVersionResponse/RegisterToolRequest/
   UpdateToolRequest/TestToolRequest/TestToolResponse/InvocationLogResponse/
   ToolType 枚举/ToolStatus 枚举/RollbackRequest）
2. src/services/tools.ts（10 端点）
3. src/stores/useToolStore.ts

【输出后必须执行】
1. 列出 10 个文件路径 + 用途
2. 输出"Tool API 端点对照表"：| 函数 | 方法 | 路径 |
3. 更新 docs/api-documentation/前端开发进度.md（Batch 5-1 ✅）
```

---

## Batch 5-2 — 工具平台（页面）

```
【Batch 5-2】工具管理页面，预计 11 个文件。

【文档锚点】
- agent-platform-API.md §工具平台（同 5-1 的 10 端点）

【生成清单】
1. src/pages/Tools/ToolListPage.tsx + index.ts（Table + 类型筛选 Select +
   注册新工具 Button AuthorizedButton permission="tool:create" +
   搜索 + 启停 Switch）
2. src/pages/Tools/ToolDetailPage.tsx + index.ts（Descriptions 详情 + 编辑 Modal +
   版本历史 Table：版本号/发布时间/变更说明 + 回滚 Button Popconfirm +
   启停操作）
3. src/pages/Tools/ToolTestPage.tsx + index.ts（参数 JSON Editor +
   调用 Button + 结果展示：输出 JSON + 耗时/Token + 错误信息）
4. src/pages/Tools/InvocationLogs.tsx + index.ts（Table + 时间范围 +
   状态筛选 success/failure + 详情 Drawer：输入/输出/耗时/Token）

【输出后必须执行】
1. 列出 11 个文件路径 + 用途
2. 自检：
   □ 版本回滚参数是否 targetVersion
   □ 测试调用请求体格式与 API 一致
3. 更新 docs/api-documentation/前端开发进度.md（Batch 5-2 ✅）
```

---

## Batch 5-3 — 任务规划与执行

```
【Batch 5-3】任务 DAG 规划 + 执行 + 状态监控，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §任务规划与执行（6 端点）：POST /api/v1/tasks/plan、
  POST /api/v1/tasks/{id}/execute、POST /api/v1/tasks/{id}/cancel、
  GET /api/v1/tasks/{id}/status、GET /api/v1/tasks/{id}/plan、
  GET /api/v1/tasks/actions
- 前端架构设计方案.md §7.3（DAG 可视化 + 节点状态色 灰/蓝/绿/红/黑）

【生成清单】
1. src/types/task.d.ts（TaskPlanResponse/TaskExecutionResponse/
   ExecutionStatus 枚举 PENDING/RUNNING/COMPLETED/FAILED/CANCELLED/
   StepResult/ActionResponse/DAGNode/DAGEdge）
2. src/services/tasks.ts（6 端点）
3. src/stores/useTaskStore.ts（executionStatus 轮询 + WebSocket task_progress 更新）
4. src/pages/Tasks/TaskPlanPage.tsx + index.ts（动作列表 + 拖拽 DAG 编排
   @xyflow/react + 参数配置 Drawer + 提交规划 Button）
5. src/pages/Tasks/ExecutionMonitor.tsx + index.ts（DAG 可视化：节点颜色
   PENDING=灰/RUNNING=蓝/COMPLETED=绿/FAILED=红/CANCELLED=黑 +
   WebSocket task_progress 实时刷新节点状态 +
   取消 Button AuthorizedButton permission="task:execute" +
   点击节点→日志 Drawer + 执行耗时 + 步骤列表）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 自检：
   □ 节点状态 5 种颜色是否与 §7.3 一致
   □ task_progress WS 消息类型是否对接
3. 更新 docs/api-documentation/前端开发进度.md（Batch 5-3 ✅）
```

---

## Batch 5-4 — 提示词管理

```
【Batch 5-4】提示词模板 CRUD + 版本管理 + Diff + 渲染，预计 16 个文件。

【文档锚点】
- agent-platform-API.md §提示词管理（去重后 14 端点）：GET/POST /api/v1/prompts、
  GET/PUT/DELETE /api/v1/prompts/{id}、POST /api/v1/prompts/{id}/publish、
  POST /api/v1/prompts/{id}/render、POST /api/v1/prompts/{id}/preview、
  POST /api/v1/prompts/{id}/rollback?targetVersion=、
  POST /api/v1/prompts/{id}/archive、GET /api/v1/prompts/{id}/versions、
  GET /api/v1/prompts/{id}/versions/{versionId}、
  GET /api/v1/prompts/{id}/diff?v1=&v2=

【生成清单】
1. src/types/prompt.d.ts（PromptTemplateResponse/PromptVersionResponse/
   CreatePromptRequest/UpdatePromptRequest/RenderRequest/RenderResponse/
   DiffResponse/VersionStatus 枚举：DRAFT/PUBLISHED/ARCHIVED）
2. src/services/prompts.ts（14 端点）
3. src/stores/usePromptStore.ts
4. src/pages/Prompts/PromptListPage.tsx + index.ts（Table + 搜索 + 新建 +
   归档 + 状态标签色：DRAFT灰/PUBLISHED绿/ARCHIVED橙）
5. src/pages/Prompts/PromptEditor.tsx + index.ts（TextArea 模板编辑 +
   变量 {{variableName}} 高亮 + 变量列表侧边栏 +
   预览渲染面板 Preview + 发布 Button）
6. src/pages/Prompts/VersionHistory.tsx + index.ts（版本列表 Table +
   查看内容 Modal + 回滚 Popconfirm + Diff 对比 → DiffViewer）
7. src/components/Prompt/DiffViewer.tsx + index.ts（两个版本差异对比：
   新增行绿色背景/删除行红色背景/修改行黄色背景 + 行号）

【输出后必须执行】
1. 列出 16 个文件路径 + 用途
2. 自检：
   □ 版本状态机 DRAFT→PUBLISH→ARCHIVED 与 API 一致
   □ Diff 端点参数 v1/v2 与 API 一致
   □ Render 端点请求体变量格式与 API 一致
3. 更新 docs/api-documentation/前端开发进度.md（Batch 5-4 ✅，Phase 5 汇总：
   Agent 能力 100%，总文件 50）
```

---

---

# Phase 6：安全治理

---

## Batch 6-1 — 安全围栏

```
【Batch 6-1】敏感词规则 CRUD + 安全事件列表，预计 12 个文件。

【文档锚点】
- agent-platform-API.md §安全围栏（8 端点）：
  敏感词：GET/POST /api/v1/security/sensitive-words、
  GET/PUT/DELETE /api/v1/security/sensitive-words/{id}、
  PATCH /api/v1/security/sensitive-words/{id}/status
  安全事件：GET /api/v1/security/events、GET /api/v1/security/events?conversationId=
- 前端架构设计方案.md §7（Security 页面占位）
- agent-platform-API.md 敏感词响应字段：id/tenantId/word/matchType(EXACT/REGEX/SEMANTIC)/
  matchTypeLabel/category(INJECTION/JAILBREAK/PII/CUSTOM)/categoryLabel/
  severity(LOW/MEDIUM/HIGH/BLOCK)/severityLabel/action(LOG/WARN/BLOCK)/
  actionLabel/status(ACTIVE/DISABLED)/statusLabel/createdAt/updatedAt

【生成清单】
1. src/types/security.d.ts（SensitiveWordResponse：精确匹配以上 16 字段 +
   CreateWordRequest/UpdateWordRequest / SecurityEventResponse）
2. src/services/security.ts（8 端点）
3. src/pages/Security/SensitiveWords.tsx + index.ts（Table + 搜索 +
   分类筛选 Select：INJECTION/JAILBREAK/PII/CUSTOM +
   严重程度筛选 Select：LOW/MEDIUM/HIGH/BLOCK +
   动作筛选 Select：LOG/WARN/BLOCK +
   新建/编辑 Modal：word/matchType/category/severity/action +
   启停 Switch + 删除 Popconfirm）
4. src/pages/Security/SecurityEvents.tsx + index.ts（Table + 时间范围 +
   会话 ID 筛选 + 详情 Drawer + 严重程度标签色）

【输出后必须执行】
1. 列出 12 个文件路径 + 用途
2. 输出"敏感词字段矩阵"：| 字段 | 枚举值 | API 来源 |
3. 自检：matchType/category/severity/action/status 所有枚举是否完全匹配 API
4. 更新 docs/api-documentation/前端开发进度.md（Batch 6-1 ✅）
```

---

## Batch 6-2 — 人机协同审批

```
【Batch 6-2】审批列表 + 审批详情 + 统计 + WS 集成，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §人机协同审批（6 端点）：GET /api/v1/approvals?filter=、
  GET /api/v1/approvals/{id}、POST /api/v1/approvals/{id}/approve、
  POST /api/v1/approvals/{id}/reject、GET /api/v1/approvals/stats
- 前端架构设计方案.md §5.4.1（WS 消息类型）
- 前后端权限对应技术方案.md §1.4（审批权限映射 approval:read/approve）

【生成清单】
1. src/types/approval.d.ts（ApprovalWorkflow/ApprovalStats/RiskLevel:low|medium|high|critical/
   RiskConfig:level/label/color/icon/animated）
2. src/services/approvals.ts（6 端点：list/filter-param my-pending|my-resolved|
   my-requested|by-status + getById/approve/reject/getStats）
3. src/stores/useApprovalStore.ts
4. src/pages/Approvals/ApprovalListPage.tsx + index.ts（Tabs：待审批/已审批/我发起的 +
   Table + 风险等级标签色 + 时间范围 + 统计卡片概览）
5. src/pages/Approvals/ApprovalDetail.tsx + index.ts（Descriptions 详情 +
   风险等级醒目展示 + 同意/拒绝 Button + 审批意见 TextArea +
   剩余时间倒计时 + 超时自动拒绝状态）
6. 更新 src/hooks/useWebSocket.ts（approval_card 消息 → 通知 +
   审批列表刷新；approval_result → 详情状态更新）
7. 更新 src/pages/Chat/ChatPage.tsx（WebSocket 接收 approval_card →
   插入 ApprovalMessage 到消息流）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 输出"审批交互场景覆盖清单"：| 场景 | 触发 | 前端表现 | 状态 |
   （新审批通知 / 同意 / 拒绝 / 超时 / 断线恢复 / 重复审批）
3. 自检：filter 参数是否支持 my-pending/my-resolved/my-requested/by-status
4. 更新 docs/api-documentation/前端开发进度.md（Batch 6-2 ✅）
```

---

## Batch 6-3 — 意图管理

```
【Batch 6-3】意图 CRUD + 单条测试 + 批量测试 + 启停，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §意图管理（去重后 8 端点）：GET/POST /api/v1/intents、
  GET/PUT/DELETE /api/v1/intents/{id}、PATCH /api/v1/intents/{id}/status、
  POST /api/v1/intents/test、POST /api/v1/intents/batch-test

【生成清单】
1. src/types/intent.d.ts（IntentResponse/CreateIntentRequest/UpdateIntentRequest/
   TestResult: input/intent/confidence/alternatives[] / BatchTestResult: total/
   correct/accuracy/matrix[][]）
2. src/services/intents.ts（8 端点）
3. src/stores/useIntentStore.ts
4. src/pages/Intents/IntentListPage.tsx + index.ts（Table + 搜索 + 新建/编辑 Modal +
   启停 Switch + 删除 Popconfirm）
5. src/pages/Intents/IntentTestPage.tsx + index.ts（输入 TextArea + 测试 Button +
   结果展示：识别意图 + 置信度 Progress + 备选列表 + 耗时）
6. src/pages/Intents/IntentBatchTest.tsx + index.ts（测试集选择 + 执行 Button +
   汇总：准确率 StatCard + 混淆矩阵 Heatmap + 逐条结果 Table）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 自检：测试结果字段 input/intent/confidence/alternatives 与 API 一致
3. 更新 docs/api-documentation/前端开发进度.md（Batch 6-3 ✅，Phase 6 汇总：
   安全治理 100%，总文件 38）
```

---

---

# Phase 7：持续优化

---

## Batch 7-1 — 反馈系统 + 优化工单 + 反馈统计面板

```
【Batch 7-1】消息反馈 + BadCase 工单 + 反馈统计，预计 14 个文件。

【文档锚点】
- agent-platform-API.md §优化工单（6 端点 + 反馈统计 1 端点）：
  GET/POST /api/v1/optimization-tickets、GET /api/v1/optimization-tickets/{id}、
  PATCH /api/v1/optimization-tickets/{id}/status、
  PATCH /api/v1/optimization-tickets/{id}/assign、
  POST /api/v1/optimization-tickets/{id}/solution、
  GET /api/v1/feedback/stats
- 前端架构设计方案.md §5.2 反馈服务（SubmitFeedbackRequest/FeedbackStats）
- 前端架构设计方案.md §7.4（反馈统计面板完整布局：4 Statistic + Line + Pie + Table）
- 前后端权限对应技术方案.md §1.4（反馈权限说明）
- 点踩 6 原因：§7.4（inaccurate/incomplete/unsafe/irrelevant/format_issue/other）

【生成清单】
1. src/types/feedback.d.ts（SubmitFeedbackRequest: messageId/conversationId/rating/
   reason?/comment? / SubmitFeedbackResponse: feedbackId/evaluationItemCreated/
   badCaseTicketId? / FeedbackStats: totalFeedbacks/positiveRate/negativeRate/
   topReasons[]/recentTrend[]/ticketCount / DislikeReason 6 枚举）
2. src/types/optimization.d.ts（OptimizationTicketResponse/TicketStatus 枚举/
   AssignRequest/SolutionRequest）
3. src/services/feedback.ts（submit/cancel/getStats）
4. src/services/optimization.ts（6 端点）
5. src/stores/useFeedbackStore.ts
6. 更新 src/components/FeedbackButtons/FeedbackButtons.tsx（对接 feedbackService.submit +
   点踩原因 6 选项 + 提交响应展示 ticketId）
7. src/pages/Optimization/TicketListPage.tsx + index.ts（Table + 状态筛选 +
   指派 Select + 提交解决方案 Modal + 关联反馈信息）
8. src/pages/Optimization/TicketDetail.tsx + index.ts（详情 + 状态流转 Timeline +
   反馈来源链接 + 解决方案内容 + 操作按钮）
9. src/pages/Optimization/FeedbackStats.tsx + index.ts（4 StatCard +
   Line 每日趋势 + Pie 原因分布 + Table 最近点踩详情 + 日期范围 DatePicker）

【输出后必须执行】
1. 列出 14 个文件路径 + 用途
2. 输出"反馈闭环完整链路"：点踩 → feedbackId → badCaseTicketId → 工单 → 评测数据集
3. 自检：
   □ DislikeReason 6 枚举与 §7.4 一致
   □ FeedbackStats 字段与 API 一致
4. 更新 docs/api-documentation/前端开发进度.md（Batch 7-1 ✅）
```

---

## Batch 7-2 — 评测数据集管理

```
【Batch 7-2】评测数据集 CRUD + 样本管理，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §评测数据集（6 端点）：GET/POST /api/v1/evaluation/datasets、
  GET/DELETE /api/v1/evaluation/datasets/{id}、
  GET /api/v1/evaluation/datasets/{id}/items、
  POST /api/v1/evaluation/datasets/{id}/items、
  DELETE /api/v1/evaluation/datasets/{id}/items/{itemId}

【生成清单】
1. src/types/evaluation.d.ts（EvaluationDatasetResponse/CreateDatasetRequest/
   DatasetItemResponse: id/datasetId/question/expectedAnswer/... +
   AddItemRequest/BatchImportRequest）
2. src/services/evaluation.ts（6 端点数据集部分）
3. src/stores/useEvaluationStore.ts
4. src/pages/Evaluation/DatasetListPage.tsx + index.ts（Table + 新建 Modal +
   删除 Popconfirm + 样本数量 Statistic + 创建时间）
5. src/pages/Evaluation/DatasetDetailPage.tsx + index.ts（样本列表 Table +
   添加样本 Modal：问题 TextArea + 期望答案 TextArea +
   批量导入 Upload：JSON/CSV 解析 +
   删除样本 + Q&A 对预览）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 自检：样本字段 question/expectedAnswer 与 API 一致
3. 更新 docs/api-documentation/前端开发进度.md（Batch 7-2 ✅）
```

---

## Batch 7-3 — 评测执行 + 精度监控

```
【Batch 7-3】评测执行 + LLM-as-Judge + 精度评估 + Grid Search，预计 13 个文件。

【文档锚点】
- agent-platform-API.md §评测执行（3 端点）：POST /api/v1/evaluation/runs、
  GET /api/v1/evaluation/runs、GET /api/v1/evaluation/runs/{id}
- agent-platform-API.md §精度监控（2 端点）：POST /api/v1/evaluation/precision/assess、
  POST /api/v1/evaluation/precision/grid-search

【生成清单】
1. 补充 src/types/evaluation.d.ts（EvaluationRunResponse: id/datasetId/status/
   metrics/createdAt / RunResultResponse: f1/precision/recall/accuracy/
   dimensionScores[]/itemResults[] / GridSearchRequest: paramGrid[]/datasetId/
   metric / PrecisionAssessRequest）
2. 补充 src/services/evaluation.ts（5 端点：评测 3 + 精度 2）
3. src/pages/Evaluation/RunResults.tsx + index.ts（评测历史 Table + 状态标签 +
   结果详情 Drawer：f1/precision/recall/accuracy StatCard +
   维度分解雷达图 + 逐条结果 Table + LLM-as-Judge 评分详情）
4. src/pages/Evaluation/PrecisionMonitor.tsx + index.ts（Grid Search 参数网格配置 +
   执行 Button + 结果表格：参数组合×指标 + 最优参数高亮 +
   精度趋势 Line Chart）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 输出"评测指标覆盖清单"：f1/precision/recall/accuracy/dimensionScores
3. 自检：评测结果字段与 API 一致 / Grid Search 参数映射与 API 一致
4. 更新 docs/api-documentation/前端开发进度.md（Batch 7-3 ✅，Phase 7 汇总：
   持续优化 100%，总文件 40）
```

---

---

# Phase 8：渠道与观测

---

## Batch 8-1 — IM 渠道管理

```
【Batch 8-1】IM 渠道配置 + 状态监控 + 企微/钉钉适配，预计 13 个文件。

【文档锚点】
- 前端架构设计方案.md §7.5（IM 渠道管理页面 + 消息类型支持矩阵）
- 前后端权限对应技术方案.md §4.4（IM 渠道安全鉴权：
  传输加密 AES-256-CBC/HTTPS + 签名验证 SHA1/HmacSHA256 +
  Token 缓存 Redis TTL 7200s + 限流 Sentinel + 日志脱敏）
- 前端架构设计方案.md §4.1 config.json im 段

【生成清单】
1. src/types/im.d.ts（ChannelConfig: channelId/type:wecom|dingtalk|feishu/
   status:connected|disconnected|unconfigured/name/icon/configJson +
   ChannelStatusBadge 颜色映射 + IMMessage 类型）
2. src/services/im.ts（渠道 API：列表/配置/测试连接/启停）
3. src/stores/useIMStore.ts
4. src/components/IM/ChannelStatusBadge.tsx + index.ts（Badge 颜色：
   已连接=#52c41a / 未配置=#ff4d4f / 预留=#d9d9d9 + Tooltip 状态说明）
5. src/pages/IM/IMConfigPage.tsx + index.ts（渠道卡片 Grid：
   企微/钉钉/飞书各一张 + 状态指示灯 + 配置表单 Drawer：
   企微:corpId/agentId/secret/Token/AESKey +
   钉钉:appKey/appSecret +
   连接测试 Button + 启用 Switch +
   消息类型支持矩阵 Table §7.5 表格 +
   安全层级展示）
6. src/pages/IM/ChannelDetail.tsx + index.ts（单渠道详情 + 回调 URL 复制 +
   Token 有效期显示 + 连接日志列表）

【输出后必须执行】
1. 列出 13 个文件路径 + 用途
2. 输出"IM 渠道安全层级检查表"：传输加密/签名验证/Token缓存/限流/脱敏
3. 自检：企微/钉钉配置字段是否完整 / 消息类型支持矩阵与 §7.5 一致
4. 更新 docs/api-documentation/前端开发进度.md（Batch 8-1 ✅）
```

---

## Batch 8-2 — 可观测性 Dashboard

```
【Batch 8-2】链路追踪 + 指标看板 + 健康检查，预计 12 个文件。

【文档锚点】
- agent-platform-API.md §健康检查 Actuator（8 端点）：/actuator/health、
  /actuator/metrics、/actuator/metrics/{name}、/actuator/prometheus、
  /actuator/info、/actuator/env、/actuator/loggers
- 前端架构设计方案.md §7（Observability 页面占位）

【生成清单】
1. src/types/observability.d.ts（TraceResponse: traceId/spanId/parentSpanId/
   serviceName/operationName/duration/timestamp/tags[]/logs[] +
   MetricsResponse: name/description/measurements[]/availableTags[] +
   HealthResponse: status/components{db/redis/milvus/minio}/details）
2. src/services/observability.ts（Actuator + 日志/链路 API）
3. src/pages/Observability/TraceListPage.tsx + index.ts（Table + 搜索 traceId +
   时间范围 + 耗时筛选 + 详情展开 Tree：span 层级 + 耗时瀑布条 +
   每个 span 的 tags/logs）
4. src/pages/Observability/MetricsDashboard.tsx + index.ts（指标选择器 +
   StatCard：CPU/内存/QPS/延迟/错误率 + 趋势 Line + 自动刷新 30s +
   时间范围切换）
5. src/pages/Observability/HealthCheck.tsx + index.ts（健康状态面板：
   整体状态 Tag + 组件红绿灯：DB/Redis/Milvus/MinIO + 详情信息 +
   上次检查时间 + 手动刷新 Button）

【输出后必须执行】
1. 列出 12 个文件路径 + 用途
2. 自检：Actuator 端点字段映射 / 健康检查组件列是否包含 DB/Redis/Milvus/MinIO
3. 更新 docs/api-documentation/前端开发进度.md（Batch 8-2 ✅，Phase 8 汇总：
   渠道与观测 100%，总文件 25）
```

---

---

# Phase 9：集成收尾

---

## Batch 9-1 — 性能优化 + 跨模块联动

```
【Batch 9-1】路由懒加载 + TanStack Query 缓存 + 跨模块联动校验，预计 16 个文件。

【文档锚点】
- 前端架构设计方案.md §八（性能优化策略含代码示例）
- 前端架构设计方案.md §8.1（代码分割：React.lazy + Suspense）
- 前端架构设计方案.md §8.4（TanStack Query 缓存示例：useQuery + staleTime 5min）
- 前后端权限对应技术方案.md §3.2（权限同步策略表）
- 前后端权限对应技术方案.md §4.5（WebSocket 实时推送权限变更）

【生成清单】
1. 更新 src/config/routes.tsx（所有页面组件改为 React.lazy(()=>import(...)) +
   Suspense fallback=<LoadingSpinner />）
2. src/components/Loading/LoadingSpinner.tsx + index.ts（Ant Design Spin 全屏居中）
3. src/hooks/useQueryCache.ts（TanStack Query QueryClient 配置：staleTime 5min +
   retry 3 + refetchOnWindowFocus false）
4. 更新 src/main.tsx（QueryClientProvider 包裹）
5. 改造 src/pages/Chat/ConversationSidebar.tsx（useQuery 缓存会话列表 §8.4）
6. 改造 src/pages/KnowledgeBase/KBListPage.tsx（useQuery 缓存 KB 列表）
7. 改造 src/pages/Users/UserListPage.tsx（useQuery 缓存用户列表）
8. 跨模块联动校验 + 联调文档：
   a. 反馈闭环：PATCH feedback → feedbackId → badCaseTicketId → Ticket → Dataset
   b. 审批链路：Tool.execute → WS approval_card → Approve/Reject → WS approval_result
   c. 权限变更：Admin 改权限 → WS permission_changed → loadUserInfo → 强制刷新
9. src/utils/performance.ts（reportWebVitals：FCP/LCP/CLS/TTI + API 耗时打点）

【输出后必须执行】
1. 列出改造文件清单 + 变更说明
2. 输出"跨模块联动测试场景"：| 场景 | 触发操作 | 预期链路 |
3. 输出"性能优化对比"：懒加载前/后 bundle 大小 + 缓存命中率
4. 更新 docs/api-documentation/前端开发进度.md（Batch 9-1 ✅）
```

---

## Batch 9-2 — 部署配置 + 最终收尾

```
【Batch 9-2】Nginx + Docker 部署 + 主题 + 最终全量汇总，预计 14 个文件。

【文档锚点】
- 前端架构设计方案.md §十（Nginx 完整配置 + Dockerfile + .dockerignore）
- 前端架构设计方案.md §十二（后续扩展：i18n 占位/主题定制/PWA/移动端）
- 前后端权限对应技术方案.md §五（权限测试清单 7 项）
- 前后端权限对应技术方案.md §六（最佳实践）

【生成清单】
1. nginx.conf（History 模式 try_files + /api/ 代理 + /ws 代理 upgrade +
   静态资源 1y 缓存 + gzip）
2. Dockerfile（node:18-alpine builder + npm ci + npm run build + nginx:alpine）
3. .dockerignore
4. README.md（项目说明/技术栈/启动方式/npm run dev / 构建部署 docker build）
5. src/assets/styles/themes/dark.less（暗色主题变量 §12.2）
6. src/assets/styles/themes/light.less（亮色主题变量）
7. src/hooks/useTheme.ts（主题切换 Hook + ConfigProvider theme）
8. src/config/i18n.ts（i18n 占位配置 §12.1：zh-CN/en-US 资源文件壳）
9. package.json scripts 完善（dev/build/preview/lint/format/test）
10. 更新 docs/api-documentation/前端开发进度.md → 最终全量汇总

【最终全量汇总输出格式】
████████████████████████████████████████████████████
  Agent Platform 前端开发 — 最终全量汇总

  总阶段：9    总批次：26
  总文件数：XXX 个

  Phase 1 基础底座     ████████████ ✅  3 批   39 文件
  Phase 2 认证权限     ████████████ ✅  3 批   45 文件
  Phase 3 核心对话     ████████████ ✅  3 批   41 文件
  Phase 4 知识引擎     ████████████ ✅  3 批   36 文件
  Phase 5 Agent 能力   ████████████ ✅  4 批   50 文件
  Phase 6 安全治理     ████████████ ✅  3 批   38 文件
  Phase 7 持续优化     ████████████ ✅  3 批   40 文件
  Phase 8 渠道观测     ████████████ ✅  2 批   25 文件
  Phase 9 集成收尾     ████████████ ✅  2 批   30 文件

  API 总端点：XXX    已对接：XXX    覆盖率：100%
  权限标识：24 种    页面覆盖：11/11    按钮覆盖：全量
  文档章节覆盖：agent-platform-API.md 100% |
                前后端权限对应技术方案.md 100% |
                前端架构设计方案.md 100%
████████████████████████████████████████████████████

【输出后必须执行 — 自动同步 7 类文件】
代码开发完成后，自动执行以下同步操作：
1. 更新 docs/开发进度.md（模块状态 + 功能清单 + API 端点 + 代码统计 + 下一步）
2. 更新 docs/project-memory/00-项目现状摘要.md（代码文件数 + 开发进度表 + 日期）
3. 更新 CLAUDE.md（当前阶段 + 文件数 + 优先级图标记 + 待完成列表）
4. 写入 docs/project-memory/XX-前端项目生成.md（实现快照：做了什么/关键决策/踩坑）
5. 写入 docs/project-memory/XX-前端项目总结-2026-06-22.md（会话总结）
6. 更新 docs/project-memory/README.md（新增会话条目）
7. 更新 memory/MEMORY.md + 独立记忆文件（关键技术决策/架构模式/踩坑）
```

---

---

# 📋 附录

## A. 每批完成后必做清单

```
【本批输出检查】
□ 文件清单（路径 + 用途一行说明）
□ 自检结果（对照文档具体章节逐项打勾）
□ 必要时输出对照矩阵（API 端点对照 / 权限覆盖 / 消息类型覆盖）

【进度同步（强制）】
□ 更新 docs/api-documentation/前端开发进度.md
  - 本批标记 ✅ + 完成日期
  - 汇总行更新：已完成批次/文件数/API 端点
□ 累计进度表更新到最新状态
```

## B. Phase 汇总检查清单（每个 Phase 结束时额外执行）

```
【Phase 汇总 — 每完成一个 Phase 额外输出】

████ Phase X 完成汇总
  批次：X-X 至 X-X（共 X 批）
  文件：XX 个
  API 端点：XX 个（列出模块）
  权限覆盖：XX 种权限标识
  文档章节覆盖：
    - agent-platform-API.md：§XX-§XX
    - 前后端权限对应技术方案.md：§XX-§XX
    - 前端架构设计方案.md：§XX-§XX
  未解决问题：无 / 列出
████
```

## C. 26 批全量索引

| 批次 | 文件数 | 模块 | 核心产出 |
|:--:|:--:|------|------|
| 1-1 | 17 | 脚手架+配置 | package.json + apiClient + config.json |
| 1-2 | 12 | 工具函数 | Logger + ErrorBoundary + routes 壳 |
| 1-3 | 10 | 布局框架 | MainLayout + PrivateRoute + NotFound |
| 2-1 | 13 | 认证 | auth.ts + useAuthStore + LoginPage |
| 2-2 | 14 | 权限基础设施 | PermissionStore + PermissionRoute + 动态菜单 + Authorized |
| 2-3 | 18 | 管理页面 | UserList + RoleManagement + PermissionConfig + TenantList |
| 3-1 | 16 | 会话服务层 | Message 联合类型 + conversation.ts + useChatStore |
| 3-2 | 9 | SSE+WS Hook | useSSE + useWebSocket + useUpload |
| 3-3 | 16 | 聊天 UI | MessageBubble + MessageList(VariableSizeList) + ApprovalCard + FeedbackButtons |
| 4-1 | 12 | KB 管理 | KBListPage + KBDetailPage + 精度配置 |
| 4-2 | 14 | 文档管理 | DocumentUpload + DocumentList + ChunkPreview |
| 4-3 | 10 | 知识检索 | SearchTest + HitRecords + SearchStrategy |
| 5-1 | 10 | 工具 Service | tool.d.ts + tools.ts + useToolStore |
| 5-2 | 11 | 工具页面 | ToolList + ToolDetail + ToolTest + InvocationLogs |
| 5-3 | 13 | 任务管理 | TaskPlan + ExecutionMonitor(DAG) |
| 5-4 | 16 | 提示词管理 | PromptList + PromptEditor + VersionHistory + DiffViewer |
| 6-1 | 12 | 安全围栏 | SensitiveWords + SecurityEvents |
| 6-2 | 13 | 审批管理 | ApprovalList + ApprovalDetail + WS 集成 |
| 6-3 | 13 | 意图管理 | IntentList + IntentTest + BatchTest |
| 7-1 | 14 | 反馈+工单 | FeedbackButtons 对接 + TicketList + FeedbackStats |
| 7-2 | 13 | 评测数据集 | DatasetList + DatasetDetail + 样本管理 |
| 7-3 | 13 | 评测+精度 | RunResults + PrecisionMonitor(Grid Search) |
| 8-1 | 13 | IM 渠道 | IMConfigPage + ChannelStatusBadge |
| 8-2 | 12 | 可观测性 | TraceList + MetricsDashboard + HealthCheck |
| 9-1 | 16 | 性能优化 | React.lazy + TanStack Query + 跨模块联动 |
| 9-2 | 14 | 部署+收尾 | nginx.conf + Dockerfile + 主题 + 全量汇总 |

## D. 如果中途想跳过某个模块

在对话中直接说：
```
跳过 Phase X Batch X-X（模块名），直接进入 Batch X-X。
请先更新 docs/api-documentation/前端开发进度.md 中对应批次标记为「已跳过」。
```
