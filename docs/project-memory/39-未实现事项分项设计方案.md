# 未实现事项分项设计方案

> **日期**: 2026-08-27
> **分支**: nacos/nacos-config
> **触发**: 架构决策

## 做了什么

- 按 `docs/已设计未实现清单.md` 的每一项编写独立实现方案。
- 目录：`docs/未实现事项设计方案/`（索引 README + A/B/C/D/E 共 46 篇方案）。
- 每篇统一：现状、场景、为何、技术栈、步骤（含类名）、流程图、验收、风险。
- B 类写明插入点（如 `StreamOrchestrationService`、`deleteWithCascade`）。

## 关键决策

- B8 作为管道编排，不重复实现 B1/B2，避免两套中间件。
- B7 推荐领域枚举 `AgentPhase` 而非强行驱动现有 Spring StateMachine（仅配置易成双源）。
- B1 输出脱敏一期完整缓冲后再展示，避免逐 token 切碎 PII。
- 第三方模式热加载不做，A2-04 一期只做租户开关+配额。

## 踩坑记录

- `EventSource` 不能带 Bearer，P5 SSE 必须 fetch 流。
- RateLimit 切面返回 `Result` 对 `SseEmitter` 无效，C8 必须特殊处理。

## 下一步

- 按 P0 接线：B1 → B3/B4 → B8。
