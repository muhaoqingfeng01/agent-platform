# 项目现状摘要

> ⚡ **每次新会话首先加载此文件** — 包含项目核心信息和当前状态
> 最后更新: **2026-08-27（A1-01 Web 聊天：`agent-platform-web/`）**

---

## 一、项目基本信息

| 项目 | 说明 |
|------|------|
| **名称** | Agent Platform（企业级 Agent 平台） |
| **路径** | `D:\mhqf_project\heavenly-craft-agent\agent-platform` |
| **架构** | DDD 六模块（common/domain/application/infrastructure/interfaces/bootstrap） |
| **构建** | Maven 多模块，JDK 17，Spring Boot 3.3.7 |
| **包名** | `com.example.agent` |

---

## 二、当前环境

| 组件 | 版本/状态 |
|------|-----------|
| JDK | 17.0.18（`C:\tools\Java\jdk-17.0.18`） |
| Maven | 使用 Aliyun 镜像 `https://maven.aliyun.com/repository/public/` |
| 本地仓库 | `D:\tools\repository` |
| 编译 | ✅ `mvn compile` 全部 7 模块通过 |

---

## 三、关键版本（已降级适配 JDK 17）

| 依赖 | 当前版本 | 说明 |
|------|----------|------|
| Java | **17** | 系统只有 JDK 17 |
| Spring Boot | **3.3.7** | 匹配 Spring Cloud 2023.0.x |
| Spring Cloud Alibaba | 2023.0.3.2 | |
| Spring AI Alibaba | 1.1.2.0 | groupId=`com.alibaba.cloud.ai` |
| Spring AI | **1.1.7** | |
| MySQL Connector | **8.0.33** | 不要用 3.0.33 或 9.x |
| Langfuse | HTTP API 直连 | 不依赖 SDK，直接调用 Ingestion API |

---

## 四、已修复的依赖坐标错误

| 原坐标（错误） | 修正后坐标 |
|----------------|-----------|
| `com.alibaba.cloud:spring-ai-alibaba-bom` | **`com.alibaba.cloud.ai:spring-ai-alibaba-bom`** |
| `com.alibaba.cloud:spring-ai-alibaba-starter` | **`com.alibaba.cloud.ai:spring-ai-alibaba-agent-framework`** |
| `org.springframework.ai:spring-ai-mcp-client-spring-boot-starter` | **`org.springframework.ai:spring-ai-starter-mcp-client`** |
| `org.springframework.ai:spring-ai-rag-core` | **`org.springframework.ai:spring-ai-rag`** |

---

## 五、项目文件结构

```
agent-platform/
├── pom.xml                                 ← 父 POM（版本管理）
├── CLAUDE.md                               ← 项目记忆入口
├── .claude/
│   ├── settings.json                       ← Stop Hook 配置
│   └── hooks/session-snapshot.py           ← 会话快照脚本
├── docs/
│   ├── project-memory/                     ← 会话记忆
│   │   ├── README.md                       ← 索引
│   │   ├── 00-项目现状摘要.md               ← 本文件
│   │   ├── 01~07-*.md                      ← 历史会话记录
│   │   └── sessions/                       ← 🆕 会话快照
│   │       ├── .session-log.jsonl          ← 追加日志
│   │       └── YYYY-MM-DD/                 ← 按日期分目录
│   ├── 开发进度.md                          ← 当前进度
│   ├── Agent平台架构设计文档.md             ← 🆕 源码级架构总入口（2026-08-26）
│   ├── 已设计未实现清单.md                   ← 🆕 2026-08-27 源码对照缺口
│   ├── 未实现事项设计方案/                   ← 🆕 一事一实现方案
│   ├── 后端开发计划.md
│   ├── 企业级Agent平台技术方案.md
│   ├── 数据库设计文档.md
│   ├── P0-基础底座/ ~ P5-交互端/            ← 技术子方案
│   ├── database/                          ← 🆕 SQL 迁移参考
│   │   ├── V1.0.0__baseline_schema.sql    ← 13 张表
│   │   ├── V1.1.0__extended_schema.sql    ← 16 张表
│   │   └── ...                            ← V1.2.0 ~ V1.5.0
├── agent-platform-bootstrap/
├── agent-platform-web/                     ← 🆕 A1-01 P5 Web 聊天（Vite+React）
│   └── src/main/resources/
│       ├── application.yml                 ← 主配置
│       ├── application-dev.yml             ← 开发环境
│       ├── application-prod.yml            ← 生产环境
│       ├── bootstrap.yml                   ← 🆕 Spring Cloud Bootstrap
│       └── logback-spring.xml              ← 🆕 日志配置
├── agent-platform-common/                  ← 共享内核
├── agent-platform-domain/                  ← 领域层
├── agent-platform-application/             ← 应用层（空）
├── agent-platform-infrastructure/          ← 基础设施层
└── agent-platform-interfaces/              ← 接口层
```

---

## 六、现有 Java 代码（573 个文件 + package-info）

| 模块 | 文件数 | 核心内容 |
|------|:--:|------|
| bootstrap | 1 | `AgentPlatformApplication.java`（`@EnableAsync`） |
| common | **27** | 6 异常类 + `Result.java` + `PageResponse` + `IdGenerator` + BizAssert + 🆕 ProjectConstants（7内部类25常量） |
| domain | **125** | 23 实体 + 23 仓储接口 + 32 值对象 + 5 安全接口 + 19 DomainService/端口 + 3 交互策略 |
| application | **157** | 19 AppService + 3 识别器 + 1 责任链 + 5 提取器 + 4 Resolver + 5 Handler + 7 切片策略 + 1 管线 + Security DTO + Event + 4 交互策略 |
| infrastructure | **138** | 23 PO + 23 Mapper + 23 Impl + 3 ServiceImpl + 13 Config + 4 Rag + 2 Observability + AgentMetrics + McpClientManager + HttpToolAdapter + 3 Annotation + 2 Aspect + 🆕 SchedulerConfig + DynamicScheduledTaskManager + RagConfig + AiModelConfig + SecurityConfig + SessionConfig + ... |
| interfaces | **125** | 21 Controller + ~102 Request/Response DTO + 1 ExceptionHandler + 1 SwaggerConfig + 4 认证 DTO |
| **合计** | **573** | P0+P1+P2(T6+T7)+P3(T10+T11)+P4(T9+T12)+P7+🆕P6(配置治理子方案01-05) 全部模块实战化，编译通过 |

---

## 七、数据库（28 张表 + V1.5.0，手动管理）

### V1.0.0（13 张基础表）✅ 已部署
t_tenant, t_user, t_role, t_permission, t_user_role, t_role_permission,
t_agent_config, t_conversation, t_message, t_knowledge_base, t_tool_registry,
t_prompt_template, t_evaluation_run

### V1.1.0（16 张扩展表）✅ 已部署
t_intent, t_long_term_memory, t_prompt_template_version,
t_task_execution, t_task_step_execution,
t_document, t_document_chunk, t_knowledge_hit_record,
t_tool_invocation_log,
t_sensitive_word, t_security_event, t_audit_log,
t_approval_workflow,
t_evaluation_dataset, t_evaluation_dataset_item, t_optimization_ticket

### V1.2.0（种子数据）✅ 已部署
管理员账号、默认角色、基础权限

### V1.2.1（业务 ID 补充）✅ 已部署
conversation/message/intent/long_term_memory 表业务 ID 字段

### V1.2.2（T7 工具调用日志业务 ID）✅ 已部署
t_tool_invocation_log 新增 invocation_id 业务 ID

### V1.3.0（切片策略 + 精度控制）✅ 已部署
t_knowledge_base 新增 8 字段，t_document 新增 6 字段

### V1.4.0（KB 文件管理升级）✅ 已部署
t_knowledge_base 新增 created_by + 数据迁移(ACTIVE→ENABLED)，
t_document 数据迁移(UPLOADING→PENDING_PARSE, READY→PARSED)，
t_document_chunk 新增 deleted 软删标记，
t_knowledge_hit_record 新增索引

---

## 八、开发进度

| 优先级 | 模块 | 完成度 |
|:--:|------|:--:|
| **P0** | 统一网关 + 多租户 | 🟢 **100%**（全部完成，2 项延期至运维阶段） |
| P1 | 意图识别 + 提示词 + 任务规划 | 🟢 **100%**（T3+T4+T5 全部完成，1 项 Redis 缓存遗漏） |
| P2 | RAG 知识库 + MCP 工具 | 🟡 **~85%**（T6 V1.4.0 + T7 V1.0.0 核心完成，5 项增强待做） |
| P3 | 安全围栏 + 人机协同 | 🟢 **~95%**（T10 4 层过滤链 + T11 审批状态机，Presidio 可选延期） |
| P4 | 全链路观测 + 效果评估 | 🟡 **核心 100%**（T9 15 项 + T12 已实现，7 项运维增强 🔜 P6） |
| P5 | 交互端构建 | 🟡 A1-01 聊天✅ |
| P6 | 迭代优化（观测增强 + 运维部署 + 🆕配置治理） | 🟢 **完成**（子方案01✅ 02✅ 03✅ 04✅ 05✅ 全部完成） |

> 详见: `docs/开发进度.md`

---

## 九、启动前必须准备的外部服务

| 服务 | 默认地址 | 用途 |
|------|----------|------|
| MySQL | `localhost:3306` | 主数据库（库名: agent_platform_dev） |
| Redis | `localhost:6379` | 缓存/会话/分布式锁 |
| Milvus | `localhost:19530` | 向量存储（RAG 必需） |
| MinIO | 未配置 | 文档存储（RAG 必需） |
| Nacos | 未配置 | 服务发现（可选，默认禁用） |

---

## 十、已知限制和注意事项

1. **MySQL Connector 使用 8.0.33** — 不要升级到 9.x
2. **Spring AI Alibaba groupId 是 `com.alibaba.cloud.ai`** — 不是 `com.alibaba.cloud`
3. **Swagger 配置在 interfaces 模块** — `OpenApiConfig.java`，路径 `/swagger-ui.html`
4. **SQL 迁移文件已移至 `docs/database/`** — 新环境部署时手动按版本顺序执行
5. **所有业务表含 tenant_id** — 多租户字段隔离
6. **Maven 使用 Aliyun 镜像** — 中建 Maven 仓库通过 `settings.xml` 配置
7. **日志配置由 logback-spring.xml 统一管理** — 不再使用 application.yml 的 `logging.*` 属性
8. **Session 快照在 `sessions/YYYY-MM-DD/` 目录** — 由 Stop Hook 自动生成
9. **P0 收尾已完成** — 4 个 RepositoryStub 已删除，ServiceImpl 已通过 @Primary 注入 RepositoryImpl，admin mock 已清除

---

## 十一、⚠️ 已知差距与待实现（2026-08-27 对照源码重核）

> 完整分类清单：`docs/已设计未实现清单.md`  
> 2026-06-18 的「8 项代码缺口」中，工具版本化 / MCP 心跳 / Redis 工具缓存 / LLM Reranker / 精度评估 API / 反馈统计 等**代码已补**；当前真正的问题是 **未接线、Stub、P5 其余交互（审批/反馈/IM）、运维未部署**。A1-01 Web 聊天已在 `agent-platform-web/` 落地。

### 当前应关注的缺口（摘要）

| 类别 | 代表项 |
|------|--------|
| **A 未开工** | P5 审批卡片/反馈/IM；P7 任务/分析/审批交互模式；SSO；对话检查点；ColBERT |
| **B 未接线** | 围栏/PII 未进 stream；意图只打日志；长期记忆只写不读；对话不走提示词模板；工具与审批未进对话；StateMachine 无驱动；TenantSqlInterceptor 未注册；精度 merge 未加载 KB；KB 删除不清 Milvus |
| **C Stub** | LDAP 降级本地；CrossEncoder 假精排；切片 512/50 硬编码；`@RateLimit` 未挂接口 |
| **D 运维** | Grafana / OTel / ELK / Alert / Docker / K8s / MinIO / Presidio 服务 |
| **E 工程债** | 仅 1 个启动测试；反馈统计路径与设计不一致 |

### 开发过程中的方案变更（5 项，不算缺口）

| # | 原方案 → 实际实现 | 方向 |
|:--:|------|:--:|
| 1 | Spring Cloud Gateway → 嵌入式 Filter | 简化 |
| 2 | Nacos 服务发现 MCP → 数据库轮询 | 简化 |
| 3 | 输出过滤器链 → 单一脱敏方法 | 简化 |
| 4 | Elasticsearch → MySQL 全文索引 | 简化 |
| 5 | 上传即自动解析 → 手动触发解析 | 增强可控性 |

> 📋 历史分析: `docs/project-memory/22-P0-P4-差距分析-2026-06-18.md`（部分条目已过时）

