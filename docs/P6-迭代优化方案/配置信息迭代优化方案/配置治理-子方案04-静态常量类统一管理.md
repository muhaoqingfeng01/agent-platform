# 配置治理 — 子方案 04：静态常量类统一管理

> **版本**: V1.0.0
> **日期**: 2026-07-14
> **批次**: 第四批（P2，消除魔法数字）
> **父方案**: [配置治理-总体设计方案](配置治理-总体设计方案.md)
> **涉及项数**: 12 项常量化 + ~20 处分页默认值统一

---

## 一、范围与目标

将不适合 Nacos 动态管理（变更后必须重启、或全环境统一、或架构级固定约束）的硬编码值，抽取到 `agent-platform-common` 模块的静态常量类中，实现：

1. **消除魔法数字**：所有引用处使用有意义的常量名
2. **单一数据源**：相同语义的常量只定义一次
3. **编译期安全**：常量变更后编译器自动发现所有引用点

---

## 二、新建常量类设计

### 2.1 类结构

```
agent-platform-common/
└── src/main/java/com/example/agent/common/constant/
    └── ProjectConstants.java          ← 新建
```

```java
// 伪代码 — 仅示意结构
package com.example.agent.common.constant;

/**
 * 项目全局静态常量 — 不适合动态配置的编译期常数.
 *
 * <p>分类原则:
 * <ul>
 *   <li>放这里: 全环境统一 / 架构级固定约束 / 变更即需重启</li>
 *   <li>放 Nacos: 运行时需调整 / A/B 测试参数</li>
 *   <li>放 YAML: 启动必需 / 环境差异大 / 敏感凭据</li>
 * </ul>
 */
public final class ProjectConstants {

    private ProjectConstants() { /* 工具类禁止实例化 */ }

    // ========== 索引构建参数（Collection 创建后不可变） ==========
    public static final class IndexBuild {
        private IndexBuild() {}
        public static final String DEFAULT_TYPE = "IVF_FLAT";
        public static final String DEFAULT_METRIC = "COSINE";
        public static final int NLIST = 128;
        public static final int HNSW_M = 16;
        public static final int HNSW_EF_CONSTRUCTION = 200;
        public static final int DISKANN_MAX_DEGREE = 56;
        public static final int PQ_NBITS = 8;
        public static final int PQ_M = 768;
        public static final int SEARCH_LIST_SIZE = 100;
    }

    // ========== 安全加密常数 ==========
    public static final class Security {
        private Security() {}
        /** BCrypt 工作因子 — 10=约100ms, 12=约400ms, 14=约1.6s */
        public static final int BCRYPT_STRENGTH = 12;
    }

    // ========== 分页默认值 ==========
    public static final class Page {
        private Page() {}
        public static final int DEFAULT_PAGE_NUM = 0;
        public static final int DEFAULT_PAGE_SIZE = 20;
        public static final int MAX_PAGE_SIZE = 100;
    }

    // ========== 线程池拒绝策略 ==========
    public static final class ThreadPool {
        private ThreadPool() {}
        /** 异步任务线程池拒绝策略 — CallerRuns 保证任务不丢失 */
        public static final String ASYNC_TASK_REJECTION = "CallerRunsPolicy";
        /** 审计日志线程池拒绝策略 — DiscardOldest 丢弃最旧日志 */
        public static final String AUDIT_LOG_REJECTION = "DiscardOldestPolicy";
    }

    // ========== 业务规模限制 ==========
    public static final class BusinessLimit {
        private BusinessLimit() {}
        /** 单知识库最大文档数 */
        public static final int MAX_DOCUMENT_COUNT = 1000;
        /** 文件上传大小上限 (bytes) */
        public static final long MAX_FILE_SIZE = 50L * 1024 * 1024;  // 50MB
        /** 文档最小分片大小 (bytes) */
        public static final long MIN_PART_SIZE = 5L * 1024 * 1024;   // 5MB
    }

    // ========== API 文档元信息 ==========
    public static final class ApiDoc {
        private ApiDoc() {}
        public static final String DEV_SERVER_URL = "http://localhost:8080";
        public static final String DEV_ENV_URL = "http://dev.agent-platform.local";
        public static final String PROD_ENV_URL = "https://api.agent-platform.local";
        public static final String LICENSE_URL = "https://agent-platform.local/license";
        public static final String CONTACT_EMAIL = "dev@agent-platform.local";
    }

    // ========== WebSocket ==========
    public static final class WebSocket {
        private WebSocket() {}
        public static final String CONVERSATION_ENDPOINT = "/ws/conversation";
        public static final int MAX_SESSION_COUNT = 10000;
    }
}
```

### 2.2 行数估算

| 内部类 | 常量数 | 估计行数 |
|--------|:-----:|:-------:|
| `IndexBuild` | 9 | ~20 |
| `Security` | 1 | ~8 |
| `Page` | 3 | ~12 |
| `ThreadPool` | 2 | ~12 |
| `BusinessLimit` | 3 | ~15 |
| `ApiDoc` | 5 | ~18 |
| `WebSocket` | 2 | ~10 |
| 类声明 + Javadoc | — | ~30 |
| **合计** | **25** | **~125** |

---

## 三、现有代码改造清单

### 3.1 索引构建参数 — 消除 `MilvusCollectionManager` 硬编码

**文件**: `infrastructure/rag/MilvusCollectionManager.java`

| 位置 | 改造前 | 改造后 |
|------|--------|--------|
| 行 406 | `"{\"nlist\":128}"` | `"{\"nlist\":" + IndexBuild.NLIST + "}"` |
| 行 408 | `"{\"M\":16,\"efConstruction\":200}"` | `"{\"M\":" + IndexBuild.HNSW_M + ",\"efConstruction\":" + IndexBuild.HNSW_EF_CONSTRUCTION + "}"` |
| 行 409 | `"{\"max_degree\":56,\"search_list_size\":100}"` | 使用 `IndexBuild.DISKANN_MAX_DEGREE` / `SEARCH_LIST_SIZE` |

> ⚠️ 如果子方案 02 先行实施，此处的 Milvus 硬编码已改为从 `RagPrecisionProperties` 读取，则无需再改。两个子方案选一个实施即可，本方案作为兜底。

### 3.2 分页默认值 — 统一 ~20 个 Request DTO

**涉及文件**：`agent-platform-interfaces/src/main/java/.../dto/request/**/*Request.java`

| 当前模式 | 改造后 |
|----------|--------|
| `@Schema(description = "页码") private Integer page = 0;` | `private Integer page = ProjectConstants.Page.DEFAULT_PAGE_NUM;` |
| `@Schema(description = "每页大小") private Integer size = 20;` | `private Integer size = ProjectConstants.Page.DEFAULT_PAGE_SIZE;` |

### 3.3 业务常量 — 消除 DomainService 中的业务规模常量

| 文件 | 当前常量 | 改造后 |
|------|----------|--------|
| `KnowledgeBaseDomainService.java` | `MAX_DOCUMENT_COUNT = 1000` | `ProjectConstants.BusinessLimit.MAX_DOCUMENT_COUNT` |
| 同上 | `MAX_FILE_SIZE = 50MB` | `ProjectConstants.BusinessLimit.MAX_FILE_SIZE` |
| `DocumentApplicationService.java` | `MIN_PART_SIZE = 5MB` | `ProjectConstants.BusinessLimit.MIN_PART_SIZE` |

### 3.4 安全常数

| 文件 | 当前常量 | 改造后 |
|------|----------|--------|
| `PasswordService.java` | `BCRYPT_STRENGTH = 12` | `ProjectConstants.Security.BCRYPT_STRENGTH` |

### 3.5 API 文档元信息

| 文件 | 当前硬编码 | 改造后 |
|------|-----------|--------|
| `OpenApiConfig.java:62` | `"http://localhost:8080"` | `ProjectConstants.ApiDoc.DEV_SERVER_URL` |
| `OpenApiConfig.java:63` | `"http://dev.agent-platform.local"` | `ProjectConstants.ApiDoc.DEV_ENV_URL` |
| `OpenApiConfig.java:64` | `"https://api.agent-platform.local"` | `ProjectConstants.ApiDoc.PROD_ENV_URL` |
| `OpenApiConfig.java:59` | `"https://agent-platform.local/license"` | `ProjectConstants.ApiDoc.LICENSE_URL` |
| `OpenApiConfig.java:56` | `"dev@agent-platform.local"` | `ProjectConstants.ApiDoc.CONTACT_EMAIL` |

### 3.6 WebSocket 端点

| 文件 | 当前硬编码 | 改造后 |
|------|-----------|--------|
| `WebSocketConfig.java:35` | `"/ws/conversation"` | `ProjectConstants.WebSocket.CONVERSATION_ENDPOINT` |

---

## 四、与已有常量的关系

项目中已存在 `IdPrefixConstants`（`common/util/IdPrefixConstants.java`），定义了业务 ID 前缀。`ProjectConstants` 与此互补：

| 常量类 | 职责 | 示例 |
|--------|------|------|
| `IdPrefixConstants` | 已有的业务 ID 前缀 | `CONVERSATION = "conv"`, `MESSAGE = "msg"` |
| `ProjectConstants`（新建） | 新增的全局静态配置常量 | `BCRYPT_STRENGTH`, `DEFAULT_PAGE_SIZE` |

两套常量并行存在，互不冲突。

---

## 五、实施原则

| 原则 | 说明 |
|------|------|
| **渐进替换** | 新代码直接用 `ProjectConstants`，旧代码在后续维护中顺手改 |
| **不强求全量** | ~20 个 Request DTO 的分页默认值逐文件改，不搞批量替换 |
| **编译即验证** | 删除原 `static final` 常量后，编译器自动标红所有引用点 |
| **禁止混用** | 同一常量禁止同时在 `ProjectConstants` 和原文件中各定义一份 |

---

## 六、验证方式

| 验证项 | 方法 | 预期 |
|--------|------|------|
| 编译通过 | `mvn clean compile` | BUILD SUCCESS |
| 引用完整 | 搜索原常量名（如 `BCRYPT_STRENGTH`） | 全项目无残留引用 |
| 功能不变 | 运行已有业务逻辑 | 行为无变化 |
| 新代码规范 | Code Review 检查 | 无新魔法数字 |

---

> 📋 **上一步**: [子方案 03](配置治理-子方案03-AI模型与会话参数Nacos动态化.md)
> 📋 **下一步**: [子方案 05](配置治理-子方案05-Sentinel规则Nacos持久化.md)
